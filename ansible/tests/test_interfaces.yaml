---
- name: TEST INTERFACES
  hosts: vr-csr
  gather_facts: false

  tasks:
    - name: validate playbook exists
      stat:
        path: "/workspace/ansible/configure-interfaces.yml"
      register: playbook_result

    - name: assert that configure-interfaces.yml exists
      assert:
        that: (playbook_result.stat.isreg is defined) and (playbook_result.stat.isreg)
        fail_msg: "FAILURE: Playbook configure-interfaces.yml does not exist."