---
- name: CONFIGURE NTP
  hosts: vr-csr
  gather_facts: false

  tasks:
    - name: configure NTP server settings
      cisco.ios.ios_ntp_global:
        config: "{{ ntp_config }}"
        state: merged