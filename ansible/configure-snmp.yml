---
- name: CONFIGURE SNMP
  hosts: newsite
  gather_facts: false

  tasks:

    - name: configure SNMP Server Settings
      cisco.ios.ios_snmp_server:
        config: "{{ snmp_config }}"
        state: merged
